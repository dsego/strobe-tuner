package pattern

// The goal is to be able to draw a repeating pattern of the strobe view.
// Experimenting by creating a gradient/pattern with opengl shaders through SDL and stretching the 1D texture in raylib.
// If I can draw a gradient to a rectangle, than I can draw a pattern.

main :: proc() {
    raylib_version()
}
